import type { Metadata } from 'next'
import Link from 'next/link'
import { getLocale, getTranslations } from 'next-intl/server'
import { getFriendLinks } from '@/lib/api'

const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://www.new-universe.cn'
export async function generateMetadata(): Promise<Metadata> { const locale = await getLocale(); const zh = locale === 'zh'; return { title: zh ? '友情链接 - 墨千' : 'Friends - Byte & Beyond', description: zh ? '值得长期访问的独立博客、创作者与产品。' : 'Independent blogs, creators, and products worth returning to.', alternates: { canonical: `${SITE_URL}/${locale}/links`, languages: { zh: `${SITE_URL}/zh/links`, en: `${SITE_URL}/en/links` } } } }
export default async function FriendsPage() { const [t, locale, links] = await Promise.all([getTranslations('friends'), getLocale(), getFriendLinks()]); const zh = locale === 'zh'; return <main className="min-h-screen bg-white px-6 py-16 dark:bg-slate-950 md:px-12 lg:px-24"><div className="mx-auto max-w-6xl"><header className="flex flex-col justify-between gap-8 md:flex-row md:items-end"><div className="max-w-2xl"><p className="mb-4 text-[11px] font-bold uppercase tracking-[.22em] text-blue-600">{t('eyebrow')}</p><h1 className="text-4xl font-bold tracking-tight text-slate-950 dark:text-white md:text-6xl">{t('title')}</h1><p className="mt-6 text-lg font-light leading-relaxed text-slate-500">{t('description')}</p></div><Link href={`/${locale}/links/apply`} className="group inline-flex shrink-0 items-center gap-3 rounded-full border border-slate-200 bg-white px-5 py-3 text-sm font-bold text-slate-800 shadow-sm transition hover:-translate-y-0.5 hover:border-blue-300 hover:shadow-lg dark:border-slate-700 dark:bg-slate-900 dark:text-white"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-blue-600 text-white transition group-hover:rotate-90">+</span>{zh ? '申请交换友链' : 'Apply for a link exchange'}</Link></header>{links.length ? <div className="mt-16 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{links.map((friend) => <article key={friend.id} className="group relative overflow-hidden rounded-[1.75rem] border border-slate-100 bg-white p-7 transition duration-500 hover:-translate-y-1 hover:border-blue-100 hover:shadow-[0_24px_60px_rgba(15,23,42,.11)] dark:border-slate-800 dark:bg-slate-900"><div className="absolute -right-10 -top-10 h-32 w-32 rounded-full bg-blue-400/0 blur-3xl transition group-hover:bg-blue-400/15" /><div className="relative flex items-start justify-between gap-4">{friend.logoUrl ? <img src={friend.logoUrl} alt="" className="h-12 w-12 rounded-2xl border border-slate-100 object-cover dark:border-slate-700" /> : <span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-blue-50 text-sm font-bold text-blue-600 dark:bg-blue-900/30">{friend.siteName.slice(0, 2).toUpperCase()}</span>}<a href={friend.siteUrl} target="_blank" rel="noopener noreferrer" aria-label={`Visit ${friend.siteName}`} className="text-slate-300 transition group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-blue-500">↗</a></div><h2 className="relative mt-7 text-xl font-bold text-slate-900 dark:text-white">{friend.siteName}</h2><p className="relative mt-3 line-clamp-3 text-sm leading-relaxed text-slate-500">{friend.description}</p>{(friend.tags?.length || friend.rssUrl) ? <div className="relative mt-6 flex flex-wrap items-center gap-2">{friend.tags?.slice(0, 3).map((tag) => <span key={tag} className="rounded-full bg-slate-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-slate-400 dark:bg-slate-800">{tag}</span>)}{friend.rssUrl && <a href={friend.rssUrl} target="_blank" rel="noopener noreferrer" className="ml-auto text-xs font-semibold text-orange-500 hover:underline">RSS</a>}</div> : null}</article>)}</div> : <section className="mt-16 rounded-[2rem] border border-dashed border-slate-200 bg-slate-50/60 px-8 py-16 text-center dark:border-slate-800 dark:bg-slate-900/40"><h2 className="text-xl font-bold text-slate-900 dark:text-white">{t('emptyTitle')}</h2><p className="mx-auto mt-3 max-w-md text-sm leading-relaxed text-slate-500">{t('emptyDescription')}</p><Link href={`/${locale}/links/apply`} className="mt-7 inline-flex rounded-full bg-slate-900 px-5 py-3 text-sm font-bold text-white transition hover:bg-blue-600">{zh ? '成为第一位友邻 →' : 'Be the first friend →'}</Link></section>}<p className="mt-12 text-xs text-slate-400">{zh ? '每一条链接都来自真实、长期的内容交流。' : 'Every link is a genuine, long-term editorial connection.'}</p></div></main> }
